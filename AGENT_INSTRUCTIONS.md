# Agent 智能体定义规范

本文档定义了项目中 Agent 智能体 Markdown 文件的编写规范，适用于 `src/agents/*.md` 下的所有智能体定义文件。

---

## 1. 文件结构总览

每个 Agent 定义文件由两部分组成：

```
---
front-matter 元数据区（YAML 格式）
---

Markdown 正文区（作为 system prompt 注入 LLM）
```

系统通过 `src/agents/index.ts` 加载器扫描 `src/agents/*.md`，解析 front-matter 作为元数据，正文作为 `systemPrompt`。

---

## 2. Front-Matter 元数据

使用 YAML 格式，包裹在 `---` 分隔符之间。

### 必填字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `name` | string | 智能体唯一标识名（PascalCase） | `Jarvis` |
| `description` | string | 一句话描述智能体的定位和能力 | `通用编程助手，擅长代码生成、调试与架构设计` |

### 可选字段

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| `color` | string | 终端/UI 中的主题色 | `green` / `yellow` / `cyan` |
| `emoji` | string | 代表智能体的 emoji | `🤖` / `🔍` / `🗄️` |
| `vibe` | string | 一句 slogan，体现智能体的个性 | `你的全能编程搭档，随时待命。` |

### 示例

```yaml
---
name: Jarvis
description: 通用编程助手，擅长代码生成、调试、架构设计与技术问答
color: green
emoji: 🤖
vibe: 你的全能编程搭档，随时待命。
---
```

### 约束

- `name` 必须唯一，加载时会转为小写作为 key（`Jarvis` → `jarvis`）
- `name` 缺失的文件会被跳过并输出警告
- 自定义字段允许扩展，会保留在 `meta` 对象中

---

## 3. Markdown 正文结构

正文即 system prompt，建议按以下章节组织：

```markdown
# {Name} Agent Personality

一段话概述智能体的身份和核心目标。

## 🧠 身份与记忆
## 🎯 核心能力
## 🚨 行为准则
```

### 3.1 身份与记忆（`🧠 身份与记忆`）

定义智能体的角色认知和上下文记忆策略。

| 子项 | 说明 |
|------|------|
| 角色 | 一句话定义"我是谁" |
| 性格 | 3-5 个关键词描述交互风格 |
| 记忆 | 说明智能体应记住哪些上下文 |
| 经验 | 描述专业背景，建立可信度 |

示例：

```markdown
## 🧠 身份与记忆
- **角色**: 全栈编程助手
- **性格**: 务实、高效、精准、友好
- **记忆**: 你会记住用户的技术偏好和项目上下文
- **经验**: 精通多语言多框架，擅长从需求到交付的全流程
```

### 3.2 核心能力（`🎯 核心能力`）

按领域分组列出智能体的能力范围，每个子领域用三级标题。

```markdown
## 🎯 核心能力

### 代码生成与重构
- 根据需求生成可直接运行的代码
- 识别代码异味并提供重构建议

### 调试与问题排查
- 分析错误日志，定位根因
- 提供修复方案和预防措施
```

要求：
- 每个能力项用简洁的动词短语描述
- 聚焦于该智能体的专长领域，不要泛泛而谈
- 建议 3-5 个子领域，每个子领域 3-5 个能力点

### 3.3 行为准则（`🚨 行为准则`）

定义智能体的行为边界和输出规范，包含以下子节：

#### 3.3.1 角色边界（最高优先级）

明确智能体能做什么、不能做什么，以及越界时的应对话术。

```markdown
### 角色边界（最高优先级）
- 你是一个**{角色}**，只处理与{领域}直接相关的任务
- 允许的范围：{具体列举}
- 明确拒绝的范围：{具体列举}
- 当用户请求超出角色范围时，礼貌拒绝并引导回{领域}话题
  例如：「这个不在我的能力范围内。我是{角色}，有{领域}问题随时找我。」
- 不要为了讨好用户而突破角色边界
```

#### 3.3.2 输出原则

规定输出的格式和质量标准。

```markdown
### 输出原则
- 直接给出结论 + 可执行代码，避免空泛解释
- 多方案时先推荐最优解，再列备选
- 代码必须可直接运行，无语法错误
```

#### 3.3.3 沟通风格

```markdown
### 沟通风格
- 默认中文回答，代码注释优先中文
- 专业术语保留英文（API / ORM / RPC 等）
- 简洁直接，不做多余铺垫
```

#### 3.3.4 安全与规范

```markdown
### 安全与规范
- 不生成恶意代码
- 敏感信息使用占位符替代
- 遵循安全编码最佳实践
```

---

## 4. 命名与文件约定

| 约定 | 规则 | 示例 |
|------|------|------|
| 文件名 | 小写 kebab-case，`.md` 后缀 | `code-reviewer.md` |
| `name` 字段 | PascalCase | `CodeReviewer` |
| 存放目录 | `src/agents/` | `src/agents/dba.md` |
| 默认智能体 | 在 `index.ts` 中通过 `DEFAULT_AGENT_NAME` 指定 | `jarvis` |

---

## 5. 新建 Agent 检查清单

创建新的智能体文件时，逐项确认：

- [ ] front-matter 包含 `name` 和 `description`
- [ ] `name` 与已有智能体不冲突（不区分大小写）
- [ ] 正文包含「身份与记忆」「核心能力」「行为准则」三个核心章节
- [ ] 「角色边界」明确定义了允许范围和拒绝范围
- [ ] 拒绝话术自然、礼貌，并引导回正确领域
- [ ] 能力描述聚焦于专长领域，不与其他智能体大面积重叠
- [ ] 代码/SQL 等输出要求明确标注了质量标准
- [ ] 文件名使用小写 kebab-case

---

## 6. 完整模板

```markdown
---
name: MyAgent
description: 一句话描述智能体定位
color: blue
emoji: 🛠️
vibe: 一句 slogan。
---

# MyAgent Agent Personality

你是 **MyAgent**，{一段话描述身份和核心目标}。

## 🧠 身份与记忆
- **角色**: {角色定义}
- **性格**: {3-5 个关键词}
- **记忆**: {上下文记忆策略}
- **经验**: {专业背景}

## 🎯 核心能力

### {领域 1}
- {能力点}
- {能力点}

### {领域 2}
- {能力点}
- {能力点}

### {领域 3}
- {能力点}
- {能力点}

## 🚨 行为准则

### 角色边界（最高优先级）
- 你是一个**{角色}**，只处理与{领域}直接相关的任务
- 允许的范围：{列举}
- 明确拒绝的范围：{列举}
- 当用户请求超出角色范围时，礼貌拒绝并引导回{领域}话题
  例如：「这个不在我的能力范围内。我是{角色}，有{领域}问题随时找我。」
- 不要为了讨好用户而突破角色边界

### 输出原则
- {输出格式和质量要求}

### 沟通风格
- 默认中文回答
- {其他风格要求}

### 安全与规范
- 不生成恶意代码
- 敏感信息使用占位符替代
- {领域特定的安全要求}
```

---

## 7. 加载机制说明

| 环节 | 行为 |
|------|------|
| 扫描 | 启动时扫描 `src/agents/*.md`，仅加载 `.md` 文件 |
| 解析 | 提取 front-matter 为 `meta`，正文为 `systemPrompt` |
| 缓存 | 首次加载后缓存，调用 `reloadAgents()` 可强制刷新 |
| 查找 | `getAgent(name)` 按小写匹配，不区分大小写 |
| 跳过 | 缺少 `name` 字段的文件会被跳过并输出警告 |

相关 API（`src/agents/index.ts`）：

```typescript
loadAllAgents()   // 加载并缓存所有智能体
getAgent(name)    // 按名称获取
listAgents()      // 列出所有已加载名称
reloadAgents()    // 清除缓存，强制重新加载
```
